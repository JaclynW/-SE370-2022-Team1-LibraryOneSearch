import java.util.List;

public interface SortingInterface {

    // logs list of sorted products before returning (prints to console)
    List<Product> improvedSort(List<Product> items, int sortingApproach);
}
